import React from 'react';

const NodeToolbar = ({ onAddNode, saveLoadProps, exportProps }) => {
  return (
    <div className="node-toolbar">
      {/* Empty toolbar - all buttons moved to left panel */}
    </div>
  );
};

export default NodeToolbar;